name: meshnet_app
description: MESHNET - Emergency Mesh Network Communication App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  # flutter_bluetooth_serial: ^0.4.0  # Web'de çalışmaz
  # permission_handler: ^11.0.1       # Web'de çalışmaz  
  # flutter_blue_plus: ^1.12.13       # Web'de çalışmaz
  crypto: ^3.0.3
  cryptography: ^2.5.0
  convert: ^3.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
